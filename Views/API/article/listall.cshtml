@using SL.Util
@{
    RequestUtil req = new RequestUtil();
    int type = req.Int("type");
    var data = SL.Data.SQL.Query("select * from Article where Type=@p0 order by Sort desc", type);

    Json.Write(new { success = true, data = data }, Output);
}
